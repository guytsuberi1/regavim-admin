# עקרונות עבודה — אפליקציות רגבים בנימין

קובץ זה מגדיר את הנחות העבודה הקבועות לכל פעולת קוד בפרויקטי רגבים בנימין
(regavim-budget, regavim-agriculture, regavim-dorm). יש לפעול לפיהן בכל משימה, בלי צורך שהמשתמש יחזור עליהן.

## עיקרון 1: שאלות הבהרה לפני שינויים מהותיים

לפני כל **תוספת, שינוי או שדרוג מהותי** — פיצ'ר חדש, שינוי עיצוב, שינוי התנהגות קיימת, שדרוג —
יש לשאול את המשתמש כמה שיותר שאלות הבהרה (בעברית, רצוי עם כלי AskUserQuestion) כדי להבין
בדיוק למה התכוון, **לפני** שכותבים קוד. לא לנחש ולא להניח.

חריג: תיקוני באג ברורים ושינויי טקסט פשוטים — לבצע ישירות בלי שאלות.

## עיקרון 2: שפת עיצוב אחידה — לפי אפליקציית "ניהול עבודה חקלאית"

כל UX, UI ועיצוב — באפליקציות קיימות ובאפליקציות חדשות — חייב להתאים לעקרונות העיצוב של
אפליקציית ניהול העבודה החקלאית ("ניהול עבודה חקלאית — רגבים בנימין"), כפי שמתועדים להלן.

**חשוב — צבעים לעומת עקרונות:** העקרונות המבניים (RTL, פונט, מבנה מסך, קומפוננטות, הדפסה)
**מחייבים תמיד**. פלטת הצבעים לעומת זאת **גמישה**: לכל אפליקציה יכולה להיות פלטה משלה.
הפלטה הירוקה שלהלן היא של האפליקציה החקלאית ומשמשת ברירת מחדל; באפליקציה חדשה או קיימת
עם זהות צבעונית אחרת — לשאול את המשתמש אילו צבעים הוא רוצה (עיקרון 1), ולמלא איתם את
אותם תפקידי צבע בדיוק.

### בסיס

- עברית, RTL: `<html lang="he" dir="rtl">`
- פונט: `font-family: "Segoe UI", "Rubik", Arial, sans-serif;` גודל בסיס 15px
- רקע עמוד בהיר ותוכן בכרטיסים לבנים עם צל עדין
- רוחב תוכן מקסימלי: 1400px ממורכז, ריפוד 18px

### תפקידי צבע (משתני CSS)

כל אפליקציה מגדירה את אותם משתנים עם אותם תפקידים; הערכים יכולים להשתנות מאפליקציה לאפליקציה.
הערכים שלהלן הם פלטת ברירת המחדל (של האפליקציה החקלאית):

```css
:root {
  --primary: #2e7d32;        /* צבע ראשי — כפתורים, טאב פעיל, הדגשות */
  --primary-dark: #1b5e20;   /* גוון כהה של הראשי — כותרות, טקסט מודגש */
  --primary-light: #e8f5e9;  /* גוון בהיר של הראשי — רקעי הדגשה, כותרות טבלה */
  --accent: #f9a825;         /* צבע משני מנוגד — הדגשות משניות */
  --bg: #f4f6f8;             /* רקע עמוד */
  --card: #ffffff;           /* רקע כרטיס */
  --border: #d6dce1;         /* מסגרות */
  --text: #1c2733;           /* טקסט ראשי */
  --muted: #6b7884;          /* טקסט משני */
  --danger: #c62828;         /* אדום — מחיקה/שגיאה */
  --radius: 10px;
  --shadow: 0 1px 3px rgba(0,0,0,.08), 0 1px 2px rgba(0,0,0,.04);
}
```

כללי שימוש בצבע (חלים על כל פלטה):

- לצבע הראשי שלושה גוונים: רגיל, כהה (לטקסט וכותרות) ובהיר מאוד (לרקעים עדינים).
- הרקע הכללי אפור-בהיר והתוכן בכרטיסים לבנים — לא רקעים צבעוניים גדולים.
- אדום שמור לפעולות מסוכנות ושגיאות בלבד.

### מבנה מסך

- **סרגל עליון (topbar)**: גרדיאנט של הצבע הראשי `linear-gradient(180deg, var(--primary), var(--primary-dark))`,
  טקסט לבן; מכיל לוגו אימוג'י + שם האפליקציה (`h1` בגודל 18px, משקל 600), טאבים של ניווט ראשי,
  וחיווי סטטוס שמירה בצד.
- **טאבים ראשיים**: כפתורים בתוך הסרגל — רקע `rgba(255,255,255,.12)`, radius 8px;
  טאב פעיל: רקע לבן וטקסט `--primary-dark` מודגש.
- **תת-טאבים (subtabs)**: כפתורי גלולה (radius 20px) עם מסגרת; פעיל: רקע בצבע הראשי וטקסט לבן.
- **כותרת עמוד (page-head)**: `h2` בגודל 20px בצבע `--primary-dark`, כפתורי פעולה באותה שורה.

### קומפוננטות

- **כפתור (btn)**: רקע בצבע הראשי, טקסט לבן, radius 8px, ריפוד 8px 14px;
  וריאנטים: `secondary` (לבן + מסגרת), `accent` (צבע משני), `danger` (לבן + מסגרת ואדום), `small`.
- **שדות טופס**: מסגרת `--border`, radius 7px; בפוקוס: `outline: 2px solid var(--primary-light)`
  ומסגרת בצבע הראשי. תוויות (`label`) קטנות (13px) בצבע `--muted` מעל השדה.
- **טבלאות (table.grid)**: רקע לבן, radius 10px, צל; כותרות עם רקע `--primary-light`
  וטקסט `--primary-dark`; יישור לימין; שורה בריחוף מקבלת רקע בהיר עדין.
- **כרטיסים (card)**: רקע לבן, radius 10px, צל עדין, ריפוד 14px.
  כרטיס נושא יכול לקבל פס עליון: `border-top: 4px solid var(--primary)`.
- **צ'יפים (chip/tag)**: רקע `--primary-light`, מסגרת בצבע הראשי, טקסט `--primary-dark`, radius 16px.
- **כרטיסי סיכום (totbar)**: כרטיסים קטנים עם מספר גדול (20px, `--primary-dark`) ותווית קטנה (`--muted`).
- **מודאל**: רקע כהה שקוף, חלון לבן `width: min(560px, 92vw)`, כותרת/גוף/תחתית מופרדים בקו.

### הדפסה

לכל מסך שרלוונטי לו פלט מודפס: `@media print` שמסתיר ניווט וכפתורים (`.no-print`),
מבטל צללים, ומחליף למסגרות `1px solid #999`. מחלקת `.print-only` לתוכן ייעודי להדפסה.

### כשמעדכנים אפליקציה קיימת שעיצובה שונה

לא להחליף את כל העיצוב על דעת עצמך — ליישר רכיבים חדשים לשפה הזו, ולהציע למשתמש
(בהתאם לעיקרון 1) האם ומתי ליישר את המסכים הקיימים.

## עיקרון 3: צ'קליסט לפני סיום — לקחים מבאגים אמיתיים

כל שורה כאן נולדה מבאג שהגיע למשתמש בפועל. **לעבור על הרשימה לפני שמצהירים שמשימה הושלמה.**

### אמינות מול המשתמש
- **לבדוק בדפדפן אמיתי לפני שמצהירים "עובד"** — לא להסיק מקריאת קוד. Chromium מותקן;
  להריץ headless, לכתוב תוצאות ל-`data-*` ולקרוא אותן. "תיקנתי" בלי בדיקה = באג שחוזר.
- **העדות של המשתמש מהשטח גוברת על ההנחה שלי.** אם הוא אומר "ראיתי שזה עובד" —
  זו עובדה, וההנחה שלי היא זו שצריכה להיבדק מחדש. (קרה: טענתי ש-AI לא מייצר עברית
  תקינה בתמונה; המשתמש הראה שכן — והוא צדק.)
- **לא להניח שהתיקון הגיע.** לוודא שה-`?v=` באמת עלה ושה-push עבר. (קרה: שינויים נתקעו
  ב-cache ישן ולא הגיעו למשתמש במשך כמה סבבים.)
- **`?v=N` לא מגן על `index.html` עצמו.** הדפדפן שומר את ה-HTML בקאש, וה-HTML הישן ממשיך
  לבקש את ה-JS הישן — אז המשתמש לא רואה **שום** שינוי, גם כשהפריסה תקינה לחלוטין.
  נפתר ב-`admin-site/version.json` + סקריפט בראש `index.html` שקורא אותו ב-`cache:'no-store'`
  ומרענן פעם אחת כשהגרסה שונה. **בכל העלאה של `?v=` יש לעדכן גם את `version.json`
  וגם את הקבוע `LOCAL` בסקריפט.** (קרה: גיא דיווח "כל השינויים לא נכנסו לאתר" בזמן ש-`gh-pages`
  הכיל את כל הקוד החדש.)
- **מחיקת בלוק קוד לפי "מכאן עד המרקר הבא" מוחקת גם מה שהתגנב לאמצע.** אחרי מחיקה כזו
  לוודא ב-`grep` שכל שם שהיה בבלוק ועדיין בשימוש קיים. (קרה: הסרת `openFyModal` מחקה גם
  את `OWN_SHEET`/`ownSheetLink` ששכנו לידו; `node --check` עבר, והגיליון יצא ריק אצל גיא.)
- **`node --check` בודק תחביר, לא ריצה.** בדיקה אמיתית = לרנדר את המסך ולספור שורות
  (`querySelectorAll('tbody tr').length`), לא רק לוודא שאין שגיאת פרסור.
- **בבדיקות דפדפן: לדרוס את הפונקציות ה*מיוצאות* שהמסך באמת קורא.** דריסת `Store.budgetState`
  לא משפיעה על `budgetCategoriesFor` שקורא ל-`budgetState()` הפנימי — הבדיקה תיראה "ריקה"
  גם כשהקוד תקין, ותשלח אותי לחפש באג שלא קיים.
- **כשמשתמש אומר "לא רואה את השינוי" — לבדוק קודם מה יש ב-`gh-pages`**
  (`git show origin/gh-pages:js/x.js | grep <הקוד החדש>`), לא להתחיל לתקן את הקוד.

### פורטלים ציבוריים (report.html, consent.html) — נפתחים מהטלפון, ברשת סלולרית חלשה
- **להציג את הטופס מיד.** אף פעם לא לחשוף UI רק אחרי שקריאת רשת חזרה —
  כל תקלה משאירה מסך תקוע על "טוען…" לנצח. נתונים נטענים ברקע.
- **לכל פעולת רשת — מגבלת זמן (timeout) ומסלול נפילה.** רשימה שלא נטענה → הקלדה חופשית.
- **כישלון משני לא מפיל את הפעולה העיקרית.** אם העלאת הקובץ נכשלה — הדיווח עדיין נשלח,
  עם הודעה ברורה למשתמש מה חסר ומה לעשות.
- **לכווץ תמונות בדפדפן לפני העלאה** (canvas, ~1600px, JPEG 0.75). צילום מהטלפון הוא
  3–8 מגה ותוקע את השליחה; אחרי כיווץ ~200KB.
- **הודעת שגיאה = הסיבה האמיתית מהשרת** + מה לעשות עכשיו. לא "אירעה שגיאה".

### אפליקציית הניהול
- **אחרי כל שינוי נתונים — `App.render()`.** בלי זה הנתון נשמר אבל המסך מציג ערך ישן,
  והמשתמש מדווח בצדק "שיניתי וכלום לא קרה".
- **לשמור מיקום גלילה בין רינדורים** (`snapshotScroll`/`restoreScroll` ב-app.js).
  בטבלאות RTL רינדור מחדש מקפיץ את הטבלה ימינה.
- **משתני צבע: `--brand*` באפליקציית התפעול, לא `--primary*`.** שימוש בשם הלא נכון נופל
  בשקט לפלטה של אפליקציה אחרת.
- **כל רכיב חדש נבדק גם ב-`body.dark` וגם ברוחב 640px.** רקעים וצבעי טקסט קשיחים
  נעלמים במצב חשוך; טבלאות רחבות נשברות בטלפון.
- **`?v=N` עולה בכל התגיות ב-index.html** אחרי כל שינוי JS/CSS.

### העלאת קבצים ל-Supabase Storage
- **שם הקובץ במפתח חייב להיות ASCII.** תווים בעברית → `Invalid key` וההעלאה נדחית.
  להמיר לשם לטיני לפני ההעלאה (`storageName()` ב-store.js וב-report.html);
  שם שכולו עברית הופך ל-`file.<ext>`. (קרה: "צו חירום נוכחי.pdf" הפיל גם דיווח של עובד וגם
  העלאה מהאפליקציה — פעמיים, כי התיקון הראשון טיפל בתסמינים ולא בסיבה.)
- **מדיניות RLS עם `to anon` לא חלה על משתמש מחובר.** דלי/טבלה שנגישים גם מפורטל ציבורי
  וגם מהאפליקציה צריכים **שתי מדיניות נפרדות** — אחת ל-`anon` ואחת ל-`authenticated`.
  (קרה: `admin-approvals` קיבל insert ל-`anon` בלבד; העלאה מהאפליקציה נכשלה ב-
  `new row violates row-level security policy`.)
- **כשאין גישה לשרת מסביבת הפיתוח — לבקש מהמשתמש את צילום השגיאה.** לתקן "תסמינים"
  בלי לראות את השגיאה האמיתית זה ניחוש; צילום מסך אחד חסך סבב שלם.

### גיט
- `git pull` לפני עבודה ו-`git push` בסוף — הריפו קיים בכמה עותקים (OneDrive + ענן).
- לאמת שהדחיפה עברה (`git ls-remote`), לא להניח.

## עיקרון 4: הקובץ הזה מתעדכן בעצמו

**אני לא זוכר בין שיחות. מה שלא כתוב כאן — נעלם.** לכן:

- **בסוף כל משימה שבה התגלה באג, מלכודת טכנית או החלטת מוצר — להוסיף שורה לקובץ הזה**,
  בלי שהמשתמש יבקש. באג → עיקרון 3. החלטה → "החלטות תוכן שכבר סוכמו". פרט טכני → "מלכודות טכניות".
- **בתחילת כל שיחה, לפני נגיעה בקוד — להצליב `git log --pretty=format:"%ad %s" --date=short`
  מול הקובץ הזה.** לחפש גיליונות/פיצ'רים שכבר נבנו ולא מתועדים (או להפך), ולתקן מיד.
  לוקח דקה ותופס פערים משיחות שאין לי אליהן גישה. (קרה: גיליון "ניהול עובדים" נבנה ב-22/7
  והיה רשום כ"נותר לבנייה" — כמעט נבנה מחדש מאפס.)
- **לכתוב את הלקח, לא את הסיפור.** שורה אחת שאפשר לפעול לפיה, ובסוגריים הבאג שגרם לה.
- **לתקן שורות שהתיישנו** במקום לערום שכבות סותרות.
- החלק המשותף (עקרונות 1–4 + "הקשר כללי") **זהה בכל הריפואים** — שינוי בו מסונכרן לכולם.

## הקשר כללי

- האפליקציות משרתות את ישיבת "רגבים בנימין"; קהל היעד אינו טכני.
- ממשק המשתמש וכל הטקסטים למשתמש — בעברית. תקשורת עם המשתמש — בעברית.
- הודעות commit יכולות להיות בעברית או באנגלית, אך תמציתיות וברורות.
- **קוד שגיא צריך להעתיק — להדביק בצ'אט עצמו**, לא לשלוח כקובץ ולא להפנות לגיטהאב.
  הוא מעתיק מהצ'אט לעורך של Supabase/הדפדפן, וכל הפניה חיצונית מוסיפה לו צעד מיותר.
  (בקשה מפורשת: "תשלח את הקוד להעתקה פה. תמיד תשלח קוד להעתקה".)

---

# הקשר הפרויקט — regavim-admin ("תפעול")

הסעיף הזה ספציפי לריפו הזה. **קרא אותו לפני כל עבודה** — הוא חוסך סבבי שאלות שכבר נעשו.

## מה זו האפליקציה
כלי הניהול האדמיניסטרטיבי של **גיא צוברי**, מנהלן ישיבת רגבים בנימין.
- אפליקציה: https://guytsuberi1.github.io/regavim-admin/ (דורש התחברות)
- פורטל דיווח לעובדים: `/report.html` (פתוח, בלי סיסמה)

## איך לעבוד עם גיא
- **עברית בלבד** — גם בממשק וגם בשיחה.
- **גיליון-גיליון**: לפני בניית גיליון חדש עושים סבב שאלות העמקה (AskUserQuestion), ורק אז כותבים קוד.
- גיא מספק **קובצי אקסל אמיתיים** כמפרט — לקרוא אותם לעומק לפני שמתכננים.
- הוא מעדיף **הזנה ישירה בטבלה** על פני חלונות קופצים: שורת "הוספה מהירה" (תיאור + Enter)
  + עריכת תאים במקום. זו התבנית המועדפת בכל גיליון חדש.
- אחרי כל שינוי — **לפרוס ולאמת שזה חי**, לא רק להצהיר שזה עובד.

## ארכיטקטורה
- Vanilla HTML/CSS/JS, **בלי build**. `admin-site/` = כל האתר. סקריפטים קלאסיים (לא modules).
- Supabase (אותו פרויקט כמו שאר אפליקציות רגבים) — Auth, PostgreSQL, Realtime, Storage.
- פריסה: push ל-`main` → GitHub Actions → ענף `gh-pages`.
- **מוסכמת cache**: אחרי כל שינוי ב-JS/CSS — להעלות את `?v=N` בכל התגיות ב-index.html.

## מודל הנתונים (טבלת `admin_state`, שורות JSONB)
| שורה | תוכן |
|---|---|
| `core` | מצבת עובדים + הגדרות (תעריפים, חתימה, רשימות תחומים/אחראים/קבלנים) |
| `lc:YYYY-MM`, `sub:YYYY-MM`, `abs:YYYY-MM`, `pstat:YYYY-MM` | דוחות חודשיים |
| `tasks` | רשימת משימות (רציפה, לא חודשית) |
| `projects` | פרויקטים + תת-משימות מקוננות |
| `portal` | שמות עובדים לפורטל (קריאה אנונימית) |
| `events` | אירועים וטיולים — לוז + צ'ק-ליסט משימות |
| `kk` | קולות קוראים + מפת הכרעות על חשבוניות מאפליקציית התקציב |
| `safety` | בטיחות, רישוי וביטוחים — אישורים עם תוקף + מסמכי רישוי מוסד |

`admin_submissions` = דיווחי עובדים מהפורטל · `event_consents` = אישורי הורים חתומים.
Storage: `admin-approvals` (אישורי מחלה/3010) · `meeting-audio` (הקלטות פגישה) · `task-files`.

## גיליונות שנבנו
1. **💼 דוחות שכר** — לוח שכר · תור אישורים · מרכז למידה · מילוי מקום · היעדרויות וגמולים · חבילת PDF
2. **👥 ניהול עובדים** — מצבת עשירה + כרטיס עובד · לוח שבועי (מבנה שעות לכל יום) ·
   מועמדים · משרות
3. **✅ ניהול משימות** — טבלה + קנבן, משימות מתחדשות
4. **🏗️ ניהול פרויקטים** — כרטיס לכל פרויקט עם פס תקציב/נוצל/מאזן + תת-משימות
5. **📣 קולות קוראים** — מחזור חיים מפרסום עד דיווח שנתי · חשבוניות נמשכות
   מאפליקציית התקציב דרך תור אישורים · תכנון הוצאות עתידיות (תת-טאב תחת "ניהול תקציב")
6. **🦺 בטיחות, רישוי וביטוחים** — מרשם אישורים עם תפוגה מחושבת + צ'ק-ליסט מסמכי רישוי
7. **💰 ניהול תקציב** — גיליון-על לכל הכסף. תת-טאבים: **גיליון ניהול · תקציב מול ביצוע ·
   חיפוש חכם** (`js/budget.js`, תצוגה חיה על נתוני אפליקציית התקציב) · **קולות קוראים** (`js/kk.js`)
   ובעתיד **גפ"ן**. אין יותר גיליון נפרד לקולות קוראים — כל מקורות התקציב יושבים כאן.
8. **🗓️ תכנון אירועים וטיולים** — לוז + צ'ק-ליסט לפי תפקידים · שליחת לוז בוואטסאפ ·
   אישורי הורים דיגיטליים · פלייר AI · יצירת טיוטה מהקלטת פגישה

9. **📊 דשבורד** — מסך הפתיחה: "מה בוער" + תמונת מצב מכל הגיליונות (`js/dash.js`)

בנוסף: 🗂️ נתוני בסיס · ⚙️ הגדרות. הניווט **דו-שכבתי** (`SHEETS` ב-app.js):
גיליון בסרגל הצד → תת-טאבים בתוך התוכן.

### פורטלים ציבוריים (בלי סיסמה)
- `report.html` — דיווח עובדים (היעדרות / נסיעות / טיול) + צירוף אישור.
- `consent.html` — חתימת הורים על אישור לאירוע (חתימת אצבע + חותמת זמן).

## החלטות תוכן שכבר סוכמו (לא לשנות בלי לשאול)
- תגיות עובד: **מורה · פנימיה · מנהלה · מתגבר** (מנהל תיכון→מורה, אם בית→פנימיה).
- לוח שכר: סטטוסים **פתוח / בתהליך / נסגר** + 6 "משימות חודשיות" בסימון ידני.
- מרכז למידה: נסיעות **אוטומטיות** = `travelKm` מכרטיס העובד × מספר ימים שונים × תעריף.
  אין הזנת נסיעות ידנית.
- מילוי מקום: "המורה הנעדר" נמשך **רק** מדוח ההיעדרויות של אותו חודש, וסיבת ההיעדרות
  מתמלאת אוטומטית מהרשומה.
- היעדרויות: שעות = מספר · תאריכים = טווח מ־עד · ניכוי = כן/לא.
- **עבודה בזמן מילואים — דוח שעות יום-יום**: לכל שורה במקטע יש כפתור שפותח לוח חודשי
  (יום · תאריך · כניסה · יציאה · סה"כ · הערות) לפי מבנה האקסל של גיא. הסה"כ מחושב מהשעות,
  ומוזרם חזרה לשדות "שעות"/"תאריכים" בשורה הראשית. מילוי אוטומטי: ימי מילואים מדוח ההיעדרויות
  (מסומנים בכחול, **בלי** שעות מראש) · שישי/שבת באפור · שעות קבועות מהלוח השבועי בכרטיס העובד.
  נשמר ב-`rec.days = { 'YYYY-MM-DD': {in,out,note} }` ומופיע כדף נפרד לכל עובד בחבילת ה-PDF.
- משימות: משימה **"קבועה"** שמסומנת "הושלם" מתחדשת — היעד מתקדם לפי התדירות והיא חוזרת ל"פתוח".
- פרויקטים: **"נוצל" = סכום כל עלויות תת-המשימות**; מאזן = תקציב − נוצל (אדום בחריגה).
- פרויקטים: **אין מחיקה — רק ארכיון**. שורה שהושלמה נצבעת ירוקה; עמודות: תיאור → אחראי →
  סטטוס → ציר זמן (פתיחה→יעד מול היום) → מסמכים.
- משימות: **תת-משימות עם פס התקדמות** ו"➕ הוספה" בכל קבוצה. משימה שהושלמה עוברת
  לארכיון אוטומטית אחרי **30 יום**. **אין** כרטיס-פריט/יומן פעילות — גיא מנהל לבד ולא צריך.
- ציר זמן ודשבורד **הוסרו מגיליון המשימות** — שמורים לדשבורד המנהלים העתידי.
- אירועים: כיווץ/פתיחה **לכל כרטיס בנפרד** (לא כפתור גלובלי) · הלוז נשלח **בוואטסאפ**
  (לא "העתקה") · לכל אירוע פלייר AI.
- אישורי הורים: קישור לחתימה דיגיטלית בוואטסאפ · **חתימת אצבע + חותמת זמן + צילום נוסח
  האישור** = הוכחה משפטית · מעקב מי חתם/מי חסר מול רשימות הכיתה.
  **לא נשמרות תעודות זהות של תלמידים** — החלטת פרטיות מפורשת של גיא.
- היעדרויות: אפשר **להעלות מסמך אישור מתוך חלון העריכה** כשהוא הגיע אחרי הדיווח;
  העלאת קובץ מעדכנת את הסטטוס אוטומטית ל"אישור התקבל".
- פורטל הדיווח: סיבת היעדרות ברירת מחדל = **מילואים** · בורר שם עובד הוא `<select>` אמיתי
  (`datalist` לא נפתח בנייד) · כפתור "שלח קישור לעובדים" בתור האישורים.
- מצבת עובדים: כולם כבר עברו קליטה · "מאויש ע"י" = בורר מועמדים שהתקבלו.
- נתוני בסיס: **טבלת תלמידים אחת שטוחה** (שם מלא · כיתה · הערות) — **הכיתות נגזרות מעמודת הכיתה**,
  שינוי כיתה מעביר את התלמיד וכיתה ריקה נעלמת. באחסון נשמר עדיין `settings.classes[{name,students}]`
  כדי שאישורי ההורים ימשיכו לעבוד. יש ייבוא מאקסל + הורדת קובץ לדוגמה.
- נתוני בסיס: **שני מרשמים עצמאיים — תלמידים וספקים**, ולכל אחד **ייבוא אקסל וקובץ לדוגמה משלו**
  (כפתור גלובלי אחד לשניהם היה מבלבל). ספק = שם · תחום · טלפון · אימייל · ח.פ/ע.מ · הערות,
  נשמר ב-`settings.suppliers` ונזרע פעם אחת מ-`settings.contractors`. בורר "מבצע" בפרויקטים
  קורא את שניהם (`supplierList()` ב-projects.js) כדי שהשמות הוותיקים לא ייעלמו.
- הרשאות: **שלוש רמות** — `admin` (הכול) · `manager` (כל הגיליונות למעט הגדרות ונתוני בסיס) ·
  `secretary` (מרכז למידה בלבד). נקבעות במסך "נתוני בסיס → הרשאות" ונשמרות ב-`settings.userRoles`
  (גובר על `emp.role` ועל `ADMIN_EMAILS`). פרטי התחברות נשלחים לעובד **בוואטסאפ**.
  יש **חיפוש** בטבלה (שם/אימייל/תפקיד), והוא מרנדר רק את הטבלה — לא `App.render()` — אחרת
  הפוקוס נופל אחרי כל תו.
- תור אישורים: "הצגת דיווחים שטופלו" מציג **רק את החודש הנוכחי** (לפי `handled_at`, בנפילה ל-`created_at`
  לרשומות ישנות) — אחרת הרשימה מצטברת בלי סוף.
- מצבת עובדים: **צבע קבוע לכל תגית** — מורה כחול · פנימיה סגול · מנהלה טורקיז · מתגבר כתום
  (`U.tagChip` ב-util.js). חל על המצבת, כרטיס העובד, צ'יפי הסינון והלוח השבועי.
- משרות: **אחוז משרה נשמר כמספר שלם עם % קבוע** (הערכים הישנים 1/0.5 הומרו פעם אחת ל-100/50) ·
  עמודת **שכר מתוקצב** · **פלייר כקובץ מצורף** (דלי `task-files`) · **גרירת שורות** לסידור ידני ·
  **מועמד שאויש במשרה אחת נעול בכל השאר** (מוצג עם שם המשרה שבה הוא מאויש).
- מועמדים: **הודעה מוכנה בכל שינוי סטטוס** — נוסח לפי סטטוס עם משתנים
  (`{שם} {משרה} {מראיין} {טלפון_מזכירה} {חתימה}`), נערך **במסך ההגדרות** ולא בקוד.
  נפתח אוטומטית ב-התעניין/הגיע לראיון/התקבל וגם בהוספת מועמד חדש; **"לא רלוונטי" רק בלחיצה
  מפורשת** על כפתור השליחה (הודעת דחייה = טעות יקרה). כל שליחה נרשמת ב-`msgLog` של המועמד
  ומוצגת כצ'יפ "נשלח <תאריך>" עם היסטוריה מלאה בריחוף. חתימה = שם + תפקיד; תיאום ראיון מול
  המזכירה כולל טלפון (`settings.secretaryPhone`).
- דשבורד מנהלים: **שתי שכבות במסך אחד** — "מה בוער" (מונים שמופיעים רק כשיש מה לטפל בו;
  אין כלום → כרטיס "הכול נקי") ומתחת ששה כרטיסי תמונת מצב לפי סדר העדיפות שגיא קבע:
  רישוי ובטיחות → מחכה לאישור שלי → השבוע הקרוב → כסף → המשימות שלי → לוח שכר.
  **כותרת הכרטיס היא הקישור לגיליון** (לא כפתור נפרד). הדשבורד הוא מסך הפתיחה.
- בטיחות: **"רישיון מוסד" הוא שורה ראשית מודגשת** בראש טבלת האישורים (תמיד ראשונה, רקע מותג,
  פס כחול), עם מונה "מסמכי הרישוי X/8" שמקפיץ לצ'ק-ליסט. `Store.ensureLicenseRow()` משלים
  אותה גם למי שכבר טען את הרשימה.

## שפת העיצוב של האפליקציה הזו — בסגנון Monday

**שפת העיצוב ארוזה כסקיל: `.claude/skills/regavim-design/`** — `SKILL.md` (הכללים ותהליך
ההמרה) · `assets/base.css` + `assets/components.css` (הקוד עצמו, מועתק מהאפליקציה החיה) ·
`assets/demo.html` (כל הרכיבים בעמוד אחד) · `references/components.md` (ה-HTML של כל רכיב).
לעדכן את הסקיל כשמשנים רכיב בסיס, ולסנכרן את התיקייה לשאר הריפואים.

עיקרון 2 קובע את המבנה; **הפלטה והטיפוגרפיה כאן הן של Monday**, לפי בקשת גיא
("זה נראה מאושן ולא מספיק אינטואיטיבי"). התקנים נלקחו מ-`mondaycom/monday-ui-style`:

- **משתני צבע: `--brand*`, לא `--primary*`.** ב-`styles.css` יש כינויים (`--primary: var(--brand)`),
  אבל **חובה להגדיר אותם מחדש גם בתוך `body.dark`** — ערך שהוצב ב-`:root` לא מתעדכן
  כשה-`--brand` משתנה. (קרה: 14 שימושים ב-`--primary*` נפלו לפלטה הירוקה של האפליקציה החקלאית.)
- **פונטים**: Figtree (לטיני/מספרים) + Noto Sans Hebrew — נטענים ב-index.html.
- **מרווחים** 4/8/16/24/32 · **radius** 4/8/16 · צללים עדינים · גובה שורה בטבלה 48px.
- **סגנון Monday הוא סגנון הבסיס, לא מחלקה נפרדת**: `table.grid`, `.btn`, שדות טופס,
  `.subtabs`, `.tag` ו-`.modal` הוגדרו מחדש — שינוי במקום אחד חל על כל הגיליונות.
  `.m-grid` נשמרה כאליאס ריק לתאימות בלבד.
- כרטיס: `.card.m-card` (שטוח, בלי פס צבע עליון) · סטטוס = **נקודת צבע** `.m-cdot`
  ליד הכותרת + גלולה מלאה `.m-status`. **לא** פס `border-top:4px` — זו התבנית הישנה.
- קומפוננטות Monday שנבנו (`.m-*` ב-styles.css): בורר סטטוס צבעוני, אווטאר אחראי,
  קבוצות מתקפלות, תת-משימות עם חץ ופס התקדמות, שורת "➕ הוספה" בתחתית כל קבוצה,
  סרגל כלים מצומצם עם פופאוברים (איחוד 11 כפתורי סינון ל-4).

## רכיבי שרת — Supabase Edge Functions ו-AI

| פונקציה | מה עושה |
|---|---|
| `meeting-to-events` | הקלטת פגישה → טיוטת אירועים (לוז + משימות) לאישור |
| `manage-users` | יצירה/איפוס/מחיקה של חשבונות התחברות. **משותפת עם אפליקציית החקלאות** — הקוד יושב ב-`regavim-agriculture/supabase/functions/manage-users`, ו-CORS מאפשר את שני האתרים |
| `event-flyer` | פרטי אירוע → תמונת פלייר מעוצבת בעברית |
| `kk-doc-to-tasks` | המסמך הרשמי של קול קורא → משימות הגשה ודיווח (טיוטה לאישור) |

- המפתח `GEMINI_KEY_ADMIN` נשמר כ-secret בפרויקט. **לעולם לא להדביק מפתח בצ'אט.**
- **דגמי תמונה**: `gemini-3-pro-image-preview` מייצר **עברית תקינה**; `gemini-2.5-flash-image`
  מייצר ג'יבריש. `event-flyer` בוחר את הדגם הטוב ביותר אוטומטית דרך ListModels + ניקוד.
- דגמי תמונה **דורשים חיוב מופעל** בחשבון (מכסת free tier = 0 → שגיאת `limit: 0`).
- **אודיו מוואטסאפ**: הדפדפן מדווח על `.mpeg` כ-`video/mpeg` ו-Gemini דוחה —
  יש למפות סיומת→mime (`mimeForAudio`). כמו כן **בלי `accept="audio/*"` בבורר הקבצים**,
  אחרת קובצי וואטסאפ מוסתרים.
- פלט מובנה: `responseMimeType` + `responseSchema`. אודיו ארוך: Files API.
- כל תהליך AI ארוך חייב **חיווי חשיבה** (`openThinking`) — דקה של שקט נראית כמו תקלה.

## הגשר לאפליקציית ניהול התקציב (regavim-budget)

שתי האפליקציות על **אותו פרויקט Supabase** (`dcnndzrdimkogfjsvcku`), ולכן גיליון קולות
קוראים **וגיליון ניהול התקציב** קוראים ישירות את נתוני התקציב — בלי ייצוא ובלי הזנה כפולה.

### מהלך "העברת ניהול התקציב לתפעול" (סוכם עם גיא, אוגוסט 26)
המנהלנים ביקשו את גיליון ניהול התקציב אצלם. **לא מעבירים נתונים** — הגיליון נבנה כתצוגה
על אותה שורת `app_state`. שלבים: (1) קריאה בלבד ✔ (2) עריכה עם כתיבה בטוחה (3) אפליקציית
התקציב הופכת לאפליקציית המזכירה — **המסכים הניהוליים יוסרו רק אחרי אישור מפורש של גיא
שהכול עבר כמו שצריך**. ההזנה נשארת אצל המזכירה בלבד.
- `js/budget.js` — שלושה מסכים: **גיליון ניהול** (תקציב/נוצל/יתרה/פס קצב/תחזית + פיזור חודשי) ·
  **תקציב מול ביצוע** · **חיפוש חכם** (חשבוניות + סיכום ספקים + סיכום עובדים באותו מסך, לפי בקשתו).
- **כתיבה ל-`app_state` רק דרך `Store.budgetPatch()`** — קריאה טרייה, שינוי נקודתי, כתיבה בחזרה.
  אפליקציית התקציב שומרת את כל המצב כגוש אחד, ולכן כתיבה על בסיס עותק ישן מוחקת חשבוניות
  שהמזכירה הזינה בינתיים.
- `Store.budgetSubscribe()` — realtime על `app_state`; חשבונית שנרשמת אצל המזכירה מופיעה מיד.
- **שנת כספים**: "נוצל" נספר **רק מתנועות שבתוך השנה** — בשתי האפליקציות (`inFy`/`fyTx` בתקציב,
  `fyTransactions()` ב-budget.js). בלי זה, ב-1/9 כל הקטגוריות נשארות בחריגה כי הן ממשיכות לספור
  את החשבוניות של השנה שעברה. מעבר שנה = שינוי שני התאריכים ב"תכנון ונתונים" בלבד.
  בראש הגיליון בתפעול יש **בורר שנה** (`Store.budgetFyYears`) — שנים נגזרות מתאריכי התנועות,
  ובחירת שנה שאינה הפעילה מסומנת "שנה קודמת — לצפייה".
- **"תקציב מול ביצוע" הוסר מהניווט** לבקשת גיא ("לא מבין מה זה נותן") — היה כפילות של
  הפסים שכבר בגיליון. הגרף "תוכנן מול בפועל לפי חודש" עבר לתחתית גיליון הניהול ומכבד את
  בורר הקטגוריה. `BudgetDashView` נשאר בקוד למקרה שנחזיר.
- **שלב 2 (עריכה) בוצע**: בגיליון הניהול אפשר לערוך ישירות שם תת-קטגוריה · תקציב שנתי · אחראי ·
  הערה · פיזור חודשי (כולל "פזר שווה") ולהוסיף תת-קטגוריה. כל שינוי עובר ב-`budgetPatch`.
  **ההזנה של חשבוניות נשארת אצל המזכירה בלבד** — כאן לא מזינים תנועות.
- **לכל שנת תקציב הקטגוריות והתקציבים שלה; שינוי בשנה אחת לא נוגע באחרת** (בקשת גיא).
  המודל: `state.categories` = השנה **הפעילה** (מה שאפליקציית התקציב עובדת איתו),
  `state.fyCats[<שנה>]` = צילום של כל שנה אחרת. במעבר שנה (גלגל השיניים ליד בורר השנה →
  `openFyModal`) השנה היוצאת נשמרת ל-`fyCats` והחדשה מקבלת עותק עצמאי, עם אפשרות לאפס סכומים.
  **שנה שאינה הפעילה היא קריאה בלבד** — אין שדות עריכה, אין "פזר שווה" ואין הוספת תת-קטגוריה.
- **שנת התקציב נקבעת לפי התאריך בלבד — היא עובדה של הלוח, לא הגדרה.**
  1/9/25–31/8/26 = 25/26, ורק ב-1/9/26 היא הופכת ל-26/27. **אין כפתור "מעבר שנה"**
  (הוסר; הוא רק בלבל). `budgetCurrentFy()` ו-`activeFy()` נגזרים מ-`new Date()`.
  בורר השנה במסכים הוא **בורר תצוגה** בלבד — לצפייה בשנה קודמת ולתכנון שנה עתידית.
  המעבר עצמו קורה מאליו ב-`ensureFyRollover()` באפליקציית התקציב: צילום השנה שהסתיימה
  ל-`fyCats`, והתכנון שהוכן לשנה החדשה הופך ל-`categories`. סמן: `state.categoriesYear`.
  (קרה פעמיים: קודם הכפתור הזיז את השנה מוקדם מדי, ואז המסך הכריז על 26/27 כ"פעילה"
  באוגוסט 26. גיא: "השנת תקציב לא תלוי בלחיצה שלי".)
- **בורר שנת הכספים משותף** לגיליון ניהול התקציב ולקולות קוראים — `App.currentFy()`/`App.setFy()`
  ב-app.js. הם תת-גיליונות של אותו גיליון, ולכן החלפה באחד מחליפה בשני.
  רשומת ק"ק בלי שדה `year` נחשבת לשנה הפעילה, אחרת רשומות ותיקות היו נעלמות מהמסך.
- **מרשם ספקים אחד** — `state.suppliers` שבנתוני התקציב. אותו מאגר שהמזכירה בוחרת ממנו,
  ונערך מ"נתוני בסיס ← ספקים" דרך `budgetPatch`. לכל ספק `aliases[]` = כל הכתיבים שנראו
  בחשבוניות; **החשבוניות עצמן אף פעם לא נכתבות מחדש.**
- **זיהוי ספק כפול: שתי רמות, ולעולם לא איחוד אוטומטי לפי דמיון.**
  `U.normSupplier` מוריד גרשיים, בע"מ/חברת, פיסוק, וממיין את המילים — מפתח זהה = איחוד
  אוטומטי. דמיון (`U.supplierSimilarity`) = **הצעה בלבד** במגש "ספקים לאישור".
  ההוכחה: "אלי איטח"/"אלי איטן" ו"נחשון טכנולוגיה"/"נחשון טכנולוגיות" מקבלים **אותו ציון 88%**,
  אבל רק אחד מהם אותו ספק. איחוד אוטומטי לפי דמיון = מיזוג שני ספקים שונים.
- **החזר לעובד הוא לא ספק.** `supplierNamesFrom` מדלג על `kind='reimburse'`, **וגם** מוציא
  כל שם שמופיע אי-פעם כמקבל החזר — כך "רז גרולמן(שכר)" שהוזן בטעות בשדה הספק לא נכנס.
  מי שמופיע רק כחשבונית אי אפשר לזהות אוטומטית, ולכן יש במגש כפתור **"לא ספק"**
  (`state.notSuppliers`). (קרה: עובדים הופיעו במרשם הספקים.)
- **לוגיקה שצריך לבדוק — להוציא לפונקציה טהורה שמקבלת נתונים כפרמטר.** אחרת הבדיקה
  מחזירה "ריק" ונראית כמו הצלחה בזמן שהיא לא הריצה כלום. (`supplierNamesFrom(txs, ignore)`.)
- **`\b` של JS לא עובד על עברית.** לסנן מילות רעש אחרי פירוק למילים, לא בביטוי רגולרי.
  (קרה: `בע"מ` ו`חברת` לא הוסרו והנרמול לא זיהה כפילויות ברורות.)
- **`node smoke.js` בשורש הריפו — להריץ לפני כל פרסום.** עובר על כל 22 המסכים, נכשל
  אם מסך זרק שגיאה או יצא ריק. **תור האישורים חייב stub ל-`loadSubmissions`** — בלי חיבור
  לענן הוא נכנס ללולאת רינדור אינסופית ותוקע את הבדיקה.
- **גיליון ניהול התקציב הוא לצפייה בלבד** (החלטת גיא). כל עריכה — קטגוריות, תקציב שנתי,
  אחראי, הערה ופיזור חודשי — נעשית במקום אחד: **נתוני בסיס ← קטגוריות תקציב**.
  `editable()` ב-budget.js מחזיר `false` תמיד. (קרה: 25/26 הייתה ניתנת לעריכה בגיליון
  ו-26/27 לא, וגיא בצדק שאל למה. שני מקומות עריכה לאותו נתון = חוסר עקביות.)
- **קטגוריות התקציב נערכות רק בתכנון של שנה עתידית** — מסך "נתוני בסיס ← קטגוריות תקציב"
  (`js/bcats.js`). **אסור לשנות סיווג בדיעבד**: השנה הפעילה וכל שנה שעברה הן קריאה בלבד,
  מאף סיבה (החלטה מפורשת של גיא). המסך מציע רק את שתי השנים הבאות, כותב ל-`state.fyCats[<שנה>]`,
  ומאפשר: שינוי שם קטגוריה ראשית (חל על כל תתי-הקטגוריות שלה) · הוספת ראשית · מחיקה ·
  עריכת תת-קטגוריה, תקציב ואחראי. בשנה ריקה יש שלוש התחלות: העתקה מהשנה הפעילה עם/בלי
  סכומים, או דף ריק. התכנון נכנס לתוקף כשמעבירים את השנה הפעילה בגלגל השיניים.
  **גיליון ניהול התקציב לא השתנה** (בקשת גיא) — שם עדיין לא נוגעים בקטגוריה הראשית.
- **קולות קוראים וגפ"ן מוצגים בגיליון הניהול כשורת סיכום בלבד** (`OWN_SHEET` ב-budget.js) —
  הפירוט יושב בתת-הגיליון הייעודי שלהם, ובשורה יש קישור אליו. גיא: "אני לא צריך פירוט… יש להם תת גיליון יעודי".
- **בגיליון ניהול התקציב אין עמודת "הערות"** (בקשת גיא) — 7 עמודות: קטגוריה · תקציב שנתי ·
  אחראי · נוצל · יתרה · ניצול מול קצב · תחזית. שדה `note` נשאר בנתונים, פשוט לא מוצג.
- **ריבועי הפיזור החודשי**: כל ריבוע = תווית חודש · **התכנון** כמספר ראשי · **פס דק** של
  הביצוע מול התכנון · "בפועל X" (או "ללא ביצוע"). חריגה = מסגרת, פס וטקסט אדומים.
  הרשת היא **עמודות קבועות** — 6 בשולחני, 4/3/2 בצר — כי 12 חודשים ב-`auto-fill` נשברו
  ל-9 ועוד 3 יתומים וזה נראה שבור.

- **כל שדה סכום עובר ב-`U.moneyInput`** (util.js): מציג `2,202,709` במנוחה, מספר נקי בפוקוס.
  לא לבנות `input` של כסף ידנית — בלי זה המספרים הארוכים בלתי קריאים.

- אפליקציית התקציב = קובץ יחיד `index.html`. מצב מלא ב-`app_state` (שורה `id='main'`, JSONB).
- `state.categories: [{main, sub, annualBudget}]` — **"קולות קוראים" היא `main`**, וכל ק"ק הוא `sub`.
- `state.transactions: [{id, kind, main, sub, amount, date, supplier, invoiceNo, description}]` — החשבוניות.
- `state.fiscalYear` = 1/9–31/8 (שנת בית ספר) — מקור האמת לשנת הכספים.
- **מפתח החיבור** = שדה `budgetSub` ברשומת הק"ק ↔ ה-`sub` בתקציב.
- **חשבונית לא נספרת ב"נוצל" עד שגיא מאשר אותה** בתור האישורים (`data.kk.invoices`),
  כי המזכירה עלולה לסווג לק"ק הלא נכון. ההכרעה נשמרת אצלנו — **לא כותבים לנתוני התקציב**.
- **סכום הק"ק מוזרם מהתקציב בכל טעינה** (לא רק ביצירה), אלא אם הוזן ידנית (`amountManual`).
  סכום שמגיע מקדם סטטוס מ"פורסם" ל"התקבל הקצבה". (קרה: ביטחון נתקע על 22,000 בזמן
  שבתקציב כבר היה 222,000 — הסנכרון מילא רק שדות ריקים.)
- שלושה מספרים לכל ק"ק: **אושר** (ידני) · **נוצל** (אוטומטי מהחשבוניות) · **מתוכנן** (ידני).
  והמספר שגיא באמת רוצה: **"נותר ללא תכנון" = אושר − נוצל − מתוכנן**.

## קולות קוראים — המסלול (סוכם עם גיא, אוגוסט 26)

כל קול קורא עובר **מסלול של שלושה שלבי-על** (`STAGES` ב-kk.js), והתוכן שמוצג בכרטיס נגזר מהשלב:
- **הגשה** (פורסם · בהכנה · הוגש) → צ'ק-ליסט משימות ההגשה. **אין תמונת כסף.**
- **מימוש** (התקבל הקצבה · מימוש) → **דף ניהול התקציב של הק"ק בלבד** (הקצבה/נוצל/מתוכנן +
  חשבוניות + תכנון קדימה). גיא: "אני רוצה שהדף ניהול תקציב יהיה רק בשלב המימוש".
- **דיווח** (בדיווח · דווח ונסגר) → צ'ק-ליסט משימות הדיווח. ב"בדיווח" מעליו **שורת כסף
  מצומצמת** (אושר/נוצל/יתרה) כי אי אפשר לדווח בלי המספרים; ב"דווח ונסגר" **דף הכסף נשאר
  פתוח במלואו** (בקשת גיא במפורש).
- סטטוס **"בדיווח"** נוסף במיוחד — בלעדיו אין איפה לתלות את משימות הדיווח.
- "לא אושר / לא רלוונטי" = מחוץ למסלול, בלי צ'ק-ליסט.

- **מקור המשימות הוא המסמך הרשמי שגיא מעלה**, לא רשימה קשיחה בקוד. לכל ק"ק שדה
  `doc:{path,name,at}` (דלי `task-files` הקיים — לא נוצר דלי חדש כדי לא לדרוש מדיניות RLS
  חדשה), וכפתור "🤖 בנה משימות מהמסמך" ששולח אותו ל-Edge Function `kk-doc-to-tasks`.
  התוצאה היא **תמיד טיוטה לאישור** — אף פעם לא נכתבת ישירות על הרשימות.
- לכל משימה: `title · help · form · link · file · done`. עזרה, קישור וקובץ מצורף
  (טופס ריק) נערכים בפאנל שנפתח ב-ⓘ, והצ'יפים שבשורה פותחים אותם בקליק.
- **הפאנל מתרענן במקום ולא דרך `App.render()`** (`refreshChips` ב-kk.js) — רינדור מלא
  באמצע עריכה סוגר את הפאנל וגונב את הפוקוס.
- כשאין מסמך יש כפתור "התחל מרשימת ברירת מחדל" (`DEFAULT_STEPS`) כדי שהשלב לא יהיה מסך ריק.
- משימות הק"ק **נשארות בתוך כרטיס הק"ק בלבד** — לא מוזרמות לגיליון "ניהול משימות" (בקשת גיא).

## גיליון בטיחות ורישוי — הרשימה של מנהל הרישוי

הרשימה הגיעה ממנהל הרישוי ומחולקת לארבע קבוצות (`SAFETY_SEED` ב-store.js):
- **שנתי**: תשתית לייעודה · טופס דיווח פנימייה · כבאות · חשמלאי מוסמך
- **5 שנים**: חשמלאי בודק · מהנדס (יציבות מבנים / תקרות תלויות / מזגנים תלויים / יבילים)
- **לפי צורך**: תברואן · סקר עצים · אישורי גז · אישור מתקני ספורט
- **רישוי מוסד** (צ'ק-ליסט מסמכים, בלי תפוגה): רשימות תלמידים/עובדי חינוך/עובדי שירות ·
  אישור שימוש במבנה · מערכת שעות · תעודת הסמכה למנהל · תשריט המתחם · היתר בנייה

- **התפוגה מחושבת אוטומטית**: הנפקה + 12 חודשים (שנתי) או + 60 (5 שנים).
  שדה `expiryManual` נועל את החישוב כשגיא מזין תפוגה ידנית.
- **התראה מ-60 יום לפני** (`WARN_DAYS`): אדום = פג · כתום = עומד לפוג · אפור = חסר · ירוק = בתוקף.
  הטבלה ממוינת לפי דחיפות, ובראש הגיליון ארבעה מונים.

## מלכודות טכניות מוכחות

- **`--primary*` מול `--brand*`** — ראה "שפת העיצוב". שם לא נכון נופל בשקט לפלטה אחרת.
- **מיקום גלילה** — כל `App.render()` בונה מחדש את ה-DOM ומאפס `scrollLeft`; בטבלת RTL
  זה "קופץ ימינה". נפתר מרכזית ב-`app.js` (`snapshotScroll`/`restoreScroll` על
  `.tbl-scroll, .kb-board, .m-toolbar, .subtabs, .kpi-row`) — אין צורך לטפל בזה בכל גיליון.
- **דגל "שמירה בעבודה" שחוסם עריכה = איבוד נתון בשקט.** `if (saving) return` בגיליון התקציב
  זרק כל עריכה שנעשתה בזמן ששמירה קודמת רצה, והרינדור שאחריה החזיר את הערך הישן —
  המשתמש ראה "עדכנתי וזה לא נשמר". הפתרון הוא **תור** (`saveQueue`), לא חסימה.
  כתיבות שחייבות להיות בטור (read-modify-write כמו `budgetPatch`) — לשרשר, לא לזרוק.
- **`App.render()` באמצע הקלדה גונב פוקוס.** אחרי שמירה אוטומטית, לרנדר רק כשהתור התרוקן
  ו-`document.activeElement` אינו שדה קלט (`renderWhenIdle` ב-budget.js).
- **רענון חלקי לא מספיק** — פונקציית `refresh()` שמרעננת רק את הטבלה משאירה סרגלי כלים
  וצ'יפים מיושנים. לקרוא ל-`App.render()`.
- **מיזוג ענן (`mergeIncoming` ב-store.js)** — שורה חדשה ברמת-רשומה (tasks/projects/events)
  חייבת להצטרף לענף המיזוג, אחרת היא נופלת ל-full-replace ונתונים נדרסים.
- **שמות קבצים ל-Storage — ASCII בלבד** (ראה עיקרון 3).
- **פעולה שתלויה במצב — לבדוק גם את הרשומות שכבר נמצאות במצב היעד.** כפתור שמוצג רק
  ל"פעיל" נעלם לגמרי מעובדים שכבר סומנו לא-פעילים בייבוא, ואין דרך להגיע לתהליך.
  (קרה: "🚪 סיום העסקה" לא הופיע לעובדים שיובאו מהאקסל עם "עזב".)
- **שדה בתוך `.row` חייב `min-width:0`** — בלעדיו הוא לא מתכווץ מתחת לרוחב התוכן,
  וקלט עם placeholder ארוך גולש מהמסגרת ומתנגש בשדה השכן (נראה רק בנייד).
- **`.kpi-row` הוא שורה פנימית, לא מכולה.** שימוש בו כעטיפה למונים נותן `align-items:baseline`
  ולכן גובה שונה לכל ריבוע. המכולה הנכונה היא `.kpi-grid`. (קרה במועמדים, במשרות ובמצבת.)
- **בורר תאריך/חודש הוא של האפליקציה, לא של הדפדפן** (`U.openPicker` ב-util.js) — מיירט כל
  `input[type=date|month]` דרך מאזין mousedown גלובלי. שינוי ערך מפעיל `input`+`change` ידנית,
  אז כל קוד קיים שמאזין ל-change ממשיך לעבוד.
- **`.subtabs button` הוא `inline-flex`** כדי שאייקון + כיתוב יישבו באותה שורה
  (ה-SVG ב-`U.ICO` הוא `display:block`).
- **`?v=N` בכל 18 התגיות ב-index.html** — לוודא שהעדכון באמת קרה
  (`grep -c '?v=N'`), לא להניח שפקודת sed הצליחה.

## גיליונות שנותרו לבנייה
**דיווח גפ"ן** — היחיד שנותר. דשבורד המנהלים נבנה (`js/dash.js`, מסך הפתיחה).
`renderTimeline` ו-`renderDashboard` שמיוצאות מ-`TasksView` עדיין לא בשימוש בדשבורד —
זמינות אם גיא ירצה ציר זמן/בלוקי משימות שם.

## מלכודות תפעוליות והרשאות
- **אי אפשר לכתוב ל-Supabase בלי סשן מחובר.** המפתח האנונימי קורא רק את שורת `portal`.
- **הרשת לפרויקט Supabase חסומה מסביבת הפיתוח** (`connect_rejected` בפרוקסי) — אי אפשר לאמת מכאן
  פריסת Edge Function או שינוי בענן. **לבקש מגיא צילום מסך** במקום לנחש (`curl` ל-`$HTTPS_PROXY/__agentproxy/status`
  מראה את החסימה).
- **`Failed to send a request to the Edge Function` = כמעט תמיד CORS, לא פריסה חסרה.**
  מקור שאינו ב-`ORIGIN_PATTERNS` מקבל כותרת CORS של מקור אחר, הדפדפן חוסם, וההודעה זהה
  לזו של פונקציה שלא קיימת. **הצעד הראשון: לבקש מגיא את הקוד שפרוס בפועל** — לא לנחש.
  (קרה: `manage-users` עברה מרשימה קשיחה לתבניות regex וכתובת אתר התפעול
  `guytsuberi1.github.io` נשמטה; בזבזנו סבבים על תיאוריות של esm.sh ו-Verify JWT.)
- **הפונקציה משותפת עם אפליקציית החקלאות** — לפני שמציעים לגיא להדביק גרסה שלמה, לוודא
  שהיא לא דורסת שינוי שהוא עשה בדשבורד. עדיף **שינוי נקודתי בקוד שלו** מאשר החלפה מלאה.
- **`SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` הם Default secrets** שמוזרקים
  אוטומטית לכל Edge Function בפרויקט — **אין מה להגדיר ידנית**. (קרה: שלחתי את גיא להגדיר את
  `SERVICE_ROLE_KEY` "כי זה החלק שהכי מפילים", והצילום שלו הראה שהוא כבר שם.)
  ייבוא נתונים לענן חייב להיעשות מדפדפן שגיא מחובר בו.
- הריפו קיים בכמה מקומות (OneDrive במחשבי גיא + סביבות ענן) — **תמיד `git pull` לפני
  עבודה ו-`git push` בסוף**, אחרת העותקים מתפצלים.
- הרשאות: `admin` (גיא) רואה הכל · `manager` (הנהלה) הכול למעט הגדרות ונתוני בסיס ·
  `secretary` (מזכירה) רואה **רק** מרכז למידה. **"הנהלה" היא גישה, לא צפייה-בלבד** — מי שיש לו
  את ההרשאה יכול גם לערוך בגיליונות שהוא רואה.
